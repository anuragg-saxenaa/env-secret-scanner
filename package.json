{
  "name": "env-secret-scanner",
  "version": "1.0.0",
  "description": "Scan .env files for secrets, git exposure, and generate .env.example",
  "type": "module",
  "bin": {
    "env-secret-scanner": "src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p ."
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4"
  }
}
